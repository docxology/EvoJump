# evojump

The EvoJump Python package: evolutionary ontogenetic trajectory analysis via
the "cross-sectional laser" metaphor. Import as `import evojump` or run its
CLI. Module details in `AGENTS.md` (agent-facing) and `docs/api_reference.rst`.
