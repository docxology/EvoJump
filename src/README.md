# src

Source code for EvoJump: the `evojump` Python package implementing the
cross-sectional "laser plane" / "jumprope" ontogenetic trajectory analysis
(data core, analytics engine, evolution sampler, trajectory visualizer, CLI).
See `evojump/` for modules and the root README for usage.
