# Archived examples

Superseded workaround scripts retained for history; not run by
`run_all_examples.py` (the discoverer only scans top-level `examples/*.py`).

- `basic_usage_fixed.py` — "fixed" variant of a basic-usage script from a
  pre-0.2.0 bug workaround era; superseded by `comprehensive_demo.py`.
- `thin_orchestrator_examples.py`, `thin_orchestrator_working.py` —
  superseded thin-orchestrator scaffolding demos.
